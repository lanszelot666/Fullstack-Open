{
  "name": "backend_basics",
  "version": "1.0.0",
  "description": "Basics of node.js",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../Base_tutorial/ && npm run build && cp -r dist ../Backend_basics",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs"
  },
  "author": "Balazs Gonter",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.4",
    "nodemon": "^3.1.0"
  }
}
